Place the **FOT-MatissePro** font files in this directory if you have a license to use them, for example:

- `FOT-MatissePro-B.otf`
- `FOT-MatissePro.ttf`

`index.html` references the bundled filename under `/fonts/`. The UI falls back to monospace if the file is missing.
