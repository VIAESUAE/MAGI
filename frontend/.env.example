# Copy to .env for local production-like tests. For GitHub Actions, set the same in workflow env.
# Public Render API origin (no path, no trailing slash).
# VITE_API_BASE=https://your-service.onrender.com
